# 理想
__**## プリフロップレンジについて、いくつかのサンプルから選択できる。**__
* 標準
* ルーズ&パッシブ
* ルーズ&アグレッシブ
* タイト&パッシブ
* タイト&アグレッシブ
* （JOPT）
* （マイクロステークス）

__**ゲーム性について、以下から選択できる。**__
* MMT
  * 20BB(トナメFT)
  * 30BB(トナメ終盤)
  * 50BB(トナメ中盤)
  * 70BB(トナメ序盤)
  * 100BB(トナメ開始)
* Ring
  * 200BB, レーキ5%, 5BBCAP
  * 100BB, レーキ5%, 5BBCAP

★上記のプリフロップレンジに対して、最もエクスプロイトできるエクスプロイトレンジを設定する
★対象レンジ vs エクスプロイトレンジ のGTOを計算できるようにする。

## 憲章
### やらないこと
* GTO WizerdやPioSolverのような正確な計算をするツールではない
* 細やかなカスタマイズを提供するツールではない
* マルチウェイでの戦い方を提供するツールではない

### 大事なこと
* 90点のGTO精度
* ライト層が、勉強やハンドの振り返りに使える
* 相手がGTO外のレンジである場合、そこからMAXでエクスプロイトするための戦略を勉強できる（★ぶっちゃけこれが一番やりたい）
  * （RustのSolverコードに踏み込んで回収する必要はあるか……？）
  * （でも、コードをみるとレンジ想定も組み込み可能であるから、Exploitavilityや表層コードの修正だけでなんとかなりそうな気がしている）
  * ★問題は、そのエクスプロイト対象のレンジ構成やプレイシミュレーションをどのように再現するか。
    * まず思いつくのはSnowieみたいに沢山のハンドからの機械学習を行うこと。
    * だけど、そもそもその大量のハンド履歴データがないし、ハンドをプレイしている人のラベリングもできていない。
    * だれかいいデータ持ってないかな……

# 動作環境
Web上から利用可能。
アプリでの作成はいったん考えないが、拡張できるような技術選定を行うこと。
(wasmの一部の技術では、アプリ上でマルチスレッドが実現できないとかなんとか)

# バックエンドサービス
ログイン必須にする。
* 過去の計算結果を参照できる。
* バックエンドのキャッシュを利用できる。
* 他人と共有＆ポスト＆議論できる。
* 要望があり、サーバーリソースに余裕があれば、サーバーリソースに計算を依頼することもできる。

## 貢献
* （計算結果のサーバー送信）を必須にする？
  * サーバーに送信できないと、履歴を保存できない。くらいにしておけばよさそう。
    * ログインすれば、履歴や他人のキャッシュ、投稿が視れる。
    * ログインしなければ、単なるフロントエンドのツール。

* off-lineでは使用できないようにする。
* サーバー側で結果の正確性が確保できて初めて、フロント側に結果を保存する。
  * サーバーが落ちてたらさすがにそれは無効でよさそう。。。




# 疑問メモ
* チャンスとはどのような概念か？ -> 純粋にカードがめくられること。


# 関数とリソースの設計情報
## Input
* カード関連
  * hero_range（ルース、タイト、標準、など）
  * villain_range（ルース、タイト、標準、など）
  * flop_cards
* ゲームポジション関連
  * hero_pos
  * villain_pos
* ポット関連
  * xbet_pot
  * pot_bb
  * effective_bb
* 条件面
  * valid_bet_sizes: list
  * villain_movement_trends（相手の特徴。ブラフ過多、過小、など。）

# 20250720 実装メモ
tomlファイルの以下の部分について。
targetをコメントアウトしないと動作しなかった。理由は本当に謎。
```
[package.metadata.component]
package = "holdem-solver:solver"
target = "holdem-solver:solver@0.1.0"
```

# 20250726 実装メモ
tomlファイルについて
* crate-type = ["cdylib"] 
  * 「このクレートは共有ライブラリとして出力すべきだ」という明示。Rustのデフォルトでは lib クレートは Rust専用（rlib）形式でビルドされ、.wasm は生成されない。
* clang, wasmtimeを依存関係に追加していると、Cコンパイラを要求される模様。
  * cargo tree で見ると、どこに書いてあるかがよくわかる。

## Wasm総合メモ
ようやくテストコードをTypescriptから実行できたので、全体像をまとめる。
* Rust側
  * wit-bindgen-rt を使用する
  * コード内での記述
    * witファイルの定義に従って読み込む。
      * `use bindings::exports::holdem_solver::host::my_host::Guest;`
    * Implしたコンポーネントは`bindings::export!(MyFunction with_types_in bindings);`のようにエクスポートする（WITの定義に従う）
  * ビルドする
    * cargo component build (-r) --target wasm32-wasip2
    * target/wasm32-wasip2/debug(release)/ 配下にwasmファイルが作成される。
* Typescript側
  * Wasmファイルのままでは使えないので、Typescript用にデコードする
    * `npm install @bytecodealliance/jco` 
    * `npx @bytecodealliance/jco transpile <wasm file> -o <output path>`
  * 普通のライブラリのように読みだす
    * `import { <Interface name (CamelCase)> } from '<ts file path>'`
    * `interfaceName.functionName()`

# 20250804 メモ
* RustからWasmを読み込もうとした
  * 本ではうまく言っていたけれど、真似してもうまくいかない。
    * どうやら、wasm32-unknown-unknownとwasm32-wasip2 の違いが問題ラシイ。
    * wasip2の場合、内部で余計なパッケージをインストールしており、それがLinkerに対応していないとかなんとか
    * wasip2が不安定と言う話も聞くし、本格的に使っていっていいのかどうか……？
* ひとまず横に置いておいて、GTOToolの実装を薦めよう。
  * テストは、Rustのテスト関数の時点で実施し、Wasmにビルドするのは最後にしよう。
  * その後も、TypescriptやWasmtime、Webappからの呼び出しで行うことにしよう。
    * （結局最後には必要になるけれど、先送りにできることもありそう）
* clang存在しない問題が再燃。キャッシュ消せばいけるかとおもいきや、cargo clearn を実行しても効果なし。
  * 前はなぜできたんだ……？


# 20250818 実装メモ
* `binfgen.export!(***)` に関して、1つのファイル（かな？）から2つ以上Exportすると、2つ目のExportが重複してエラーになるので注意
```
interface game-manager {
  resource game-resource {
    new: static func(number: u32) -> game-resource;
    write: func(number: u32);
    read: func() -> u32;
    up: func() -> u32;
    down: func() -> u32;
  }
}
```
* 上記のようなWITを作成した場合、
  * impl game_manager::Guest for MyGame
  * impl game_manager::GuestGameResource for MyGame
  * の2つを実装する必要がある
* というより、実際にbinding.rsを見ながら実装する方が100倍よい


# 20250823 メモ
わかったこと
Game中のBetサイズオプションを決定しているもの
　tree_configインスタンス時に格納している、Betoptionによる。

tests.rsを中心に解析
　finalize：計算が終わった時に、gameをスターティング状態にさせるもの……？
　　★GTO計算結果がどこに組み込まれているのかがわからない（次の課題）

with_configで、設定をPostfropGame(game)にロードできる

game.playは、現在のゲームステータスによって、プレイヤーのアクションと、カードが配られるイベントの2つの意味を持つ

残りやるべきこと
　GTO計算結果がどこに格納されていて、それは取り出すこと、参照することが可能なのか？
　　⇒ファイル保存は、gameをそのままFileSaveしている。
　現在のステータスにおける、アクション比率はどこから確認すればよいのか？

現在のWebアプリを見ていて気付いたが、アクションごとのEVが表示されていない。
機能としては今後追加実装したい。

# 20250829 実装メモ
WITには借用（&self）と可変借用（&mut self）の違いを定義しておらず、自動作成されたメソッドでは可変借用ができないことが分かった。
そのため、「内部可変性」を使用するか、所有権そのものをやり取りするかの2択の方法を取る必要があった。
* 所有権 (T) … 値を破棄する責任者。
* 借用 (&T) … 読み取り専用の一時的なアクセス。
* 可変借用 (&mut T) … 一時的な排他的アクセス（同時に1つだけ）。

いったん、内部可変性を使用する方針で実装する。

# 20250830 実装メモ
WIT経由でSolver-coreの関数を起動できるようになった。
次は、game内のいろいろな情報が取得できないかコード解析、検証を行っていく。
知りたいこと一覧
* 選択によるレンジの変遷と取得
* ゲームの現在ステータス取得
  * ★`GameNode`を参照するとわかりそう＆実装できそう
* Tree情報の取得
  * Webapp画面のように、途中の選択を参照したり、途中からやり直したりするためにはどうすればいいのか？
    * 任意のNodeを取り出せるようにする必要がある。
  * これも`GameNode`から取得するのか？　それとも別だろうか……。
    * GameNodeとTreeの役割の違いを明確に理解したい
* 演算結果をファイル出力する方法

added_lines, removed_lines それぞれの意味はなにか？
strategyとは？　アクションごとに取りうる確率

MutexGuard:
  メモリを共有して複数の所有権を許可するような機能。
  Rcを使用して、複数の参照元をカウントしつつ、別のスレッドに渡せるようにする。
Box:
  リソースをスタックではなくヒープに格納するためのもの


# 20250913 開発メモ
## deepwikiより、設計情報について
評価値の更新方法について
* Webapp画面で現在とは別のTreeに移動するたびに、apply_historyが呼び出されているよう。
* 評価値についてはフロント側でキャッシュを持っておらず、都度Wasm側に問い合わせている
  * ただし、River OOP から Flop IP をクリックして過去のアクション評価値を参照した時、そのブランチへのAction履歴だけは保持しているようで、即座に再計算ができる。

## 上記を受けて必要な実装について
* 評価値取得関数を実装する
  * その時点での評価値一覧を出力。フロントエンドで表示
* 現在のコミュニティカードを取得できるようにする
* apply_history, get_history, の2関数をインターフェースとして実装する必要がある
* ゲーム初期化関数を実装する必要がある(まあその前に、これらを取得できるようにはしておきたい。最初は固定でOKかと)
  * プレイヤーのレンジ
  * ポットサイズ
  * エフェクティブスタック


# 20250915 メモ
## 次にやること
* 1ゲームを完遂する
  * お互いのハンドレンジと戦略を可視化する
  * Check以外の選択肢を用意する
  * 現在の残存確率と、次のアクション比率を表示させたい
    * 現在の残存確率は`normalized_weights`
    * 次のアクション比率は`strategy`
    * をそれぞれ実行すればいい
  * 選択可能なアクションのみボタンをアクティブにする。
  * Panicが発生した場合もコードを終了させない（ゲーム終了状態でCheckするなどした場合）
  * ハンドレンジは、All=1 ではない形にする（とても狭いレンジでいいので、テスト用に作成する）

## 上記を受けて、具体的なTodo
* WeightおよびStrategyを取得して、13x13のセルに表示させる機能を開発
  * EVの出力は今度でよい
* Betサイズオプションの指定で、Check, Fold, Bet(33%)　の3種類のみを許容する（Raiseは実装しない）
* 過去のNodeにジャンプできる機能を付ける
  * 画面上部に、現在のアクション履歴を可視性高く、かつHistory要素を持たせて実装すればよい
  * Bet, Check, Foldアクションは、上部のHistoryとは別に、ボタンで実装してよい
* ハンドレンジのテスト用初期値を用意する。
  * 用意するのは今後のことを考えてフロント側にする
  * レンジには、ハンド数10個くらいにしたい（4betpot想定）
* 最終的なEVの実現を出力する

# 20251004
やること
* 現在のゲーム状況をキチンと確認できるようにする
* エラーになってもその選択を無効にして、ゲームを続行可能にする
* 現在の選択肢一覧を出力する
* Wasmからゲームの進行を受け取り、それを13*13マスに反映させる仕組みを整える

## 実装したこと
* Witで定義した型である`WitAction`とRust側にもともと存在した`Action`を、Fromトレイトを実装することで変換可能にした

## 発生した問題
* 現在の構成だと、メモリが足りないよう。
  * メモリ不足になる要因はいくつか存在し、「アクション数」「ハンドレンジ」「allocate_memory設定」がある。
    * アクション数：多すぎると「Too many nodes」のエラーメッセージとともに終了する
    * ハンドレンジ：広すぎると「Memory usage exceeds maximum size」のメッセージとともに終了する
    * （それぞれのエラーの箇所はメッセージで検索すれば見つかる）
    * allocate_memory = true とすることで使用メモリを半減できるが、現在はハンドレンジを全レンジに広げているためか、それでも足りないよう

## Todo
メモリをより大きく確保する方法を探す。ブラウザで動かすからメモリが足りなくなっている可能性もあるが、そのあたりも含めて検証する。

## 1つの解決策
* 現状のシステムでは、コールやチェックの頻度はf32で保持している。しかしここまでの精度が必要ないのであれば、例えば20%刻みでの戦略とした場合、その戦略数は【0%, 20%, 40%, 60%, 80%, 100%】の6通りとなる。これは`u8`サイズで保持できるものであり、圧縮モードの「u16(16bit)」に比べて4bitで保持可能になる。
  * ただその場合は、u8(8bit)の中に2つ分の戦略情報を格納する必要があり、処理が難解かつBitpackのオーバーヘッドが発生する。
  * 現実的なラインとして、u8サイズで1%刻みにすれば手っ取り早くメモリ量を半減できる

## Todo
* レンジ部分の可視化
  * HandCellの修正（引数としてマーク付きの数字を渡す必要がある）
  * 現在の戦略値取得
  * 戦略値のパース作業
* Betアクションの実行を実装する（現在の可能なアクションを可変ボタン化）
* 現在のコミュニティカードを表示する
* 現在のチップ数を表示する
* Dealアクションを一覧から選択できるようにする

# 20251012 実装メモ
* フロントエンドに置いて、HandCell部分の変数受け取りを修正
* 次は、HandRange及びDefaultHandRangeを修正して、データ受け取り関数まで完成させる

# 20251013 実装メモ
* フロントエンド側で、ハンドレンジの実装
* 型変換周辺を重点的に実装。エラーなくrun dev まではできるようになった
* ここで大きな問題発生
  * RustからやってくるActionの`bet<u32>`とフロントエンドで想定していた`Bet<u32>`が違うことが分かった
  * Rust側では数値はBetサイズ、フロント側ではBet％を想定していた
  * 今後の方針
    * ソルバー内部をいじるのは難しそう
    * Wasm側のAPIで修正する（フロント側で実際のBetサイズと差異が出ないか？）
    * フロント側でBetサイズを％に直す（できそうだが、型の修正はとても面倒そう）
  * 疲れたので続きはまた今度。

# 20251026 実装メモ
* 型の問題を修正（と言うより開発）
* 現状の問題点
  * 操作性が悪い
  * 新しいハンドレンジに更新した際、以前のデータが残ってしまっている

# 20251109 実装メモ
* 操作性向上のため、History表示機能の開発開始
  * 煩雑な操作はすべてWasm側に任せる方針
  * APIの実装ができた。しかし、Potサイズが0のまま固定になっている
* 具体的な実装問題はGithubのIssueにて管理

# 20251201 実装メモ
* 基本的な課題はIssueに移行したので、ココには書かないようになる
* wasmの復元についてわかったこと
  * Typescriptから呼び出す際は、jcoを使用して、wasmファイルのみから復元できる
    * ざっくり型推論。Witファイルがいらない。
  * Rustから呼び出す際は、Witファイルを併用して厳密に呼び出す。
    * 厳密型推論。Witファイル、もしくはbindings.rsが必要になる