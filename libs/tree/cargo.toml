[package]
name = "tree"
version = "0.1.0"
edition = "2021"

[dependencies]
postflop-solver = { workspace = true }

[lib]
crate-type = ["cdylib"]

[package.metadata.component]
package = "tree"
target = "../wit/tree.wit"